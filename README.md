# ayuda

`ayuda` is a lightweight R package designed to simplify and streamline common tasks.

## Installation

You can install the development version of `ayuda` from GitHub:

```r
install.packages("devtools")
devtools::install_github("dimagmehanic/ayuda@prod")
```
