# ayuda

`ayuda` is a lightweight R package designed to simplify and streamline common tasks. It provides helpful utilities to make your workflow faster and more efficient.

## Installation

You can install the development version of `ayuda` from GitHub:

```r
install.packages("devtools")
devtools::install_github("username/repo@branch-name")
